package pos;

import pos.bonus.GetAnotherCardBonus;
import pos.bonus.IncrementBonus;
import pos.cards.Card;
import pos.cards.Cards;
import pos.games.Player;
import pos.resources.Resource;
import pos.resources.Resources;
import pos.resources.resourceList.MilitaryPoint;

public class mainTest {
	
	public static void main(String[] args){
	
	
	
		
		
		
		
		
	/*
	 Player p1 = new Player();	
	 Dice dice =  new Dice(Colors.BLACK);
	 FamilyMemberSpace familyMemberSpace = new FamilyMemberSpace(3);
	 
	 dice.rollDice();
	 
	 CardsAttivatorZone harvest = new CardsAttivatorZone(Cards.TERRITORY,familyMemberSpace);
	 FamilyMemberBonus bonus = new FamilyMemberBonus(Cards.BUILDING, +3);
	 harvest.getPreFamilyMemberCheck().attach(bonus);
	 FamilyMember familyMember = new FamilyMember(p1,dice);
	 
	 harvest.placeInSingleArea(familyMember);
	 */
	 
	 
	}
	
	
	public static void stampa(Integer testo){
		System.out.println(testo);
	}
	public static void stampa(String testo){
		System.out.println(testo);
	}
}
